# Dependencies are managed in requirements.txt.
# This file holds tool config only (ruff, pytest).

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
